# 安全说明

## 当前状态

当前版本是本地单人工作台，不应直接暴露到互联网，也不应作为已完成安全审查的多人服务使用。

## 报告安全问题

请不要通过公开 Issue 发布密码、密钥、真实数据或可以直接利用的漏洞细节。请使用 GitHub 仓库的私密安全报告功能联系仓库维护者。

报告中建议包含：

- 受影响的页面或接口
- 可以复现问题的最小步骤
- 可能影响的数据范围
- 建议的修复方式

## 本地数据

- `.env`、数据库备份和个人原始资料不得提交到 GitHub。
- 不要将 `localhost:3000`、`localhost:8000` 或数据库端口直接映射到公网。
- AI 服务密钥只能放在本地环境配置或正式密钥管理服务中。
- 公开部署前必须验证不同账号之间的数据隔离。

底层 Plane 安全问题可以同时参考上游项目的安全公告，但本仓库的定制功能需要在本仓库单独修复和验证。
